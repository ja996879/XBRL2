<%= form_for(@xbrltag) do |f| %>
  <% if @xbrltag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@xbrltag.errors.count, "error") %> prohibited this xbrltag from being saved:</h2>

      <ul>
      <% @xbrltag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tagname %><br>
    <%= f.text_field :tagname %>
  </div>
  <div class="field">
    <%= f.label :tagmean %><br>
    <%= f.text_field :tagmean %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
